# Changelog

## 1.2.0 - 2026-09-23

### 新增

- 新增文字编辑器：支持普通 TextAsset 与可识别的 FairyGUI 文字字段，可查找、批量替换、恢复原文，并在作品集中查看修改前后内容。普通文本保留编码、BOM 和换行，JSON/XML 写入前检查格式；不支持的二进制、压缩或未知 FairyGUI 格式保持只读。
- 独立 Texture2D 序列帧支持导入 GIF、APNG、动画 WebP 和 PNG 帧目录，按原帧数与尺寸适配；动画图片容器支持同格式替换。候选预览展示实际适配后的效果，保存草稿后仍可查看前后对照。
- 「动画」中的单条 Sprite 动画轨 AnimationClip 可按真实引用顺序、关键帧时长播放。已用真实资源验证 `Cry` / `Hit`（30 帧、1 秒）和 `Walk-back`（24 帧、0.8 秒），包含编译后的 Sprite 曲线与网格图集帧。
- 支持导入同源 `.animbin`，或替换动画关联图集，并预览支持的片段。浏览、制作和作品集统一提供播放/暂停、逐帧、进度、倍速与大图查看。

### 修复与校验

- 修复 AnimationClip 入口只显示静态图集的问题；无法完整播放的骨骼、多轨、变换或场景依赖动画会显示原因，加载失败可重试。
- 修复共享图集 Sprite 预览中的透明区域碎片，并按统一画布对齐各帧。
- 连续修改文字始终以原资源的格式、编码为准，修复普通文本改成 JSON/XML 后无法改回、中文编码漂移的问题；移除文字修改时逐字节恢复原始内容。
- 旧动画图集草稿无法播放时保留前后静态参考，并显示原因。
- 保存文字、动画和贴图只更新作品集草稿，安装需主动触发；同包连续编辑保留其他已修改对象，移除单项时恢复该资源。
- 增加文字、动画替换和 AnimationClip 预览自检，以及真实资源副本的隔离验证；检查失败不覆盖草稿，验证游戏原件哈希不变。尚未在游戏进程中验证修改后的最终表现。
- 修复多文件 Mod 的缓存不完整时仍能显示启用成功的问题；缺少当前游戏所需的资源时保留禁用状态，并提示重新安装。
- 为 PR 添加安全自检，并要求发布构建先通过同一套检查。
- 源码安装说明改为 Python 3.11+，与当前 NumPy 依赖要求一致。

## 1.1.0 - 2026-08-30

### 新增

- 支持检测 Steam 与 TapTap 版本的《吉星派对》。
- 新增动态图像、序列帧、FairyGUI 资源的浏览、预览与导出。
- 新增角色标注、批量标注、资源筛选和自适应分页。
- 序列帧动画支持以无损 30fps APNG 导出。

### 修复与安全

- 保留热更新 `__data` 逻辑包名映射和旧 Mod 迁移，避免卡片资源再次漏扫。
- 本地 API 加入随机会话令牌、同源检查和严格 JSON 校验，阻止跨站请求触发游戏文件操作。
- 收紧 GitHub Actions 发布权限，固定构建 Actions 与主要依赖版本。

## 1.0.20

### 新增

- 序列帧动画支持导出 **无损 30fps APNG 动画**：
  - 在「动态图像」选择序列帧动画组后，主导出按钮变为“导出 APNG”；
  - 副导出按钮仍可导出首帧 PNG；
  - APNG 使用 PNG 编码，不损失画质。

## 1.0.19

### 新增

- 预览缓存复用：
  - 已经生成过的预览图再次访问时直接加载，不再重复解包提取；
  - 源文件未变化时会自动使用已有预览。
- 新增「刷新资源」按钮：
  - 在预览区点击可强制重新提取当前资源的预览；
  - 用于游戏文件被 Mod 修改或预览异常时刷新。

## 1.0.18

### 新增

- 动态图像拆分为子组件：
  - 全部
  - 序列帧动画组
  - FairyGUI
- 新增动态资源校验：
  - 在「动态图像」页面点击“校验动态资源”按钮触发；
  - 校验过程显示进度与等待动画；
  - 校验完成后自动隐藏找不到实际资源的动态动画/组件。

## 1.0.17

### 新增

- FairyGUI 动效预览与贴图更改：
  - 在「动态图像」中选择 `包名/组件名`（如 `DisplayCard_fui/CutIn`、`Home_fui/Home_Com_Banner`）时，会自动定位到该 FairyGUI 包对应的 atlas 贴图并显示预览；
  - 可直接进入「制作替换」替换该 atlas 贴图，实现不改动效逻辑的换皮；
  - 如果暂时找不到对应 atlas，仍会显示所属 FairyGUI 包与组件说明，便于后续扩展。

## 1.0.16

### 修复

- 修复动态图像扫描不到部分动态卡牌、横幅的问题：
  - FairyGUI 包（`*_fui`）内的动效/组件名现在也会进入「动态图像」索引；
  - 例如 `HandCard_fui/effectCardSwitch`、`DisplayCard_fui/CutIn`、`Home_fui/Home_Com_Banner` 等；
  - 选择这类资源会显示所属 FairyGUI 包和组件说明，导出时仍导出整个 fui 包。
- 动态图像候选补充 `AnimationClip` 与 `AnimatorController`：
  - 动画片段和动画控制器也会出现在「动态图像」中，便于发现由 Animator 驱动的动态资源。
- 同步更新动态扫描测试脚本，新增 FairyGUI 内部动效/组件统计。

## 1.0.15

### 修复

- 修复 TapTap 版资源扫描不全的问题：
  - 之前热更新 catalog 会错误地过滤基础包/旧版目录中未进入热更 catalog 的资源；
  - 现在每个资源目录只使用自己的 catalog 过滤；
  - 基础包/旧版目录没有 catalog 时保留全部 bundle，避免漏资源。

## 1.0.14

### 修复

- 修复全屏大图预览仍不居中的问题：
  - 强制将图片 `transform-origin` 设置为左上角 `0 0`；
  - 避免缩放时把 translate 偏移一起放大，导致小窗口/大图偏移。

## 1.0.13

### 修复

- 修复全屏大图预览在非最大化/小窗口下不居中的问题：
  - 改为按实际窗口客户区尺寸计算初始缩放与居中；
  - 不再受异常 stage 尺寸影响，小窗口下也能正确适配。

## 1.0.12

### 修复

- 修复部分大图在 `img.complete` 但尚未解码完成时打开预览不居中的问题：
  - 等待图片真正取得 `naturalWidth/naturalHeight` 后再计算适配尺寸；
  - 确保边缘不超出窗口且初始居中。

## 1.0.11

### 修复

- 修复超清大图在全屏预览时初始缩放/居中不准确的问题：
  - 等待弹窗布局完成后再计算适配尺寸；
  - 保证图片边缘不超出窗口，并且初始状态居中显示。

## 1.0.10

### 修复

- 修复动态序列帧在全屏大图预览中不动的问题：
  - 大图打开时只初始化一次视图，切换帧不再触发缩放重置/归中；
  - 全屏预览同样按 30fps 播放序列帧。
- 修复大图预览中缩放/平移被帧切换重置的问题：
  - 固定图片布局尺寸，切帧时保持缩放后的图片对齐。

### 新增

- 角色表支持运行时自动释放：
  - 程序启动时如果 exe 所在目录没有 `角色表.csv`，会自动从内置资源释放一份；
  - 用户可以直接修改 exe 旁的 `角色表.csv`，下次启动生效。
- 角色表新增完整分类/标注项：横幅、卡面、卡背、地块、事件、货币、筹码、头像、buff、地图预览、表情、骰子、成就、主题、活动、道具、UI、光照、其他。
- 启动时自动清理已失效的角色标注：
  - 如果角色表中删除了某个角色，已标注为该角色的资源会自动重置为未标注；
  - 如果角色表中新增了角色/分类，会直接出现在标注下拉菜单中。

## 1.0.9

### 新增

- 浏览资源排序改为 **Bundle / 资源名** 双排序：
  - 先按 Bundle 一个文件一个文件地排；
  - Bundle 内部再按资源名排序。
- 同名资源不再合并，不同 Bundle 中的同名资源会全部展示出来。
- 新增角色标注功能：
  - 支持为单个资源标注角色；
  - 支持一键将整个 Bundle 标注为同一角色；
  - 未标注资源排在前面，已标注资源排在后面；
  - 支持按角色筛选资源（需先手动标注）。
- 读取项目根目录 `角色表.csv` 作为角色/怪物名单。
- 序列帧动画预览按 **30fps** 播放：
  - 浏览小窗支持序列帧动画预览；
  - 点击大图预览时同样按 30fps 播放；
  - 大图缩放/平移时保持帧与帧之间的对齐。

## 1.0.8

### 新增

- 浏览资源新增「动态图像」类型：
  - 自动索引并展示序列帧贴图组、视频、FairyGUI 动态 UI 包、Live2D/Spine/DragonBones/GIF/WebP/APNG 候选。
  - 序列帧贴图组会显示组名、帧数、帧号范围和首帧预览。
  - 视频 / FairyGUI 等资源会显示资源类型与简要说明。
  - 动态资源支持导出：序列帧导出首帧 PNG，其它动态资源导出原始字节。
- 底层新增 `astral_party_auto/modkit/dynamic.py`：
  - 集中管理动态 2D 资源识别规则，方便后续扩展新引擎/新格式。
  - 索引结构增加 `dynamic` 类型，索引版本升级到 6，刷新索引后生效。
- 新增本地动态 2D 资源扫描测试脚本：
  - `tests/dynamic_scan/scan_dynamic_images.py`
  - 可扫描本机游戏资源，统计 Unity 资源种类，并输出序列帧、视频、FairyGUI、Live2D/Spine/GIF 等候选清单。

## 1.0.7

### 修复

- 修复“一键创建去SFW贴图Mod”提示找不到资源的问题：
  - 实际游戏中 `_sfw` 贴图与同名无 `_sfw` 贴图往往不在同一个资源包；
  - 现在会跨资源包查找无后缀源贴图，再用源贴图像素替换 `_sfw` 贴图。
- 新增本地资源扫描测试脚本：
  - `tests/sfw_scan/scan_sfw_textures.py`
  - 可用来扫描本机游戏资源，验证 `_sfw` 与无后缀贴图的配对情况。

## 1.0.6

### 新增

- 贴图预览支持全屏大图：
  - 点击任意预览小图可打开全屏大图预览；
  - 右上角 X 或点击空白区域关闭；
  - 鼠标滚轮以鼠标位置为中心缩放；
  - 支持拖拽平移查看放大后的细节。

## 1.0.5

### 修正

- 一键去 SFW 功能改为只处理**贴图**，不再处理模型：
  - 扫描所有以 `_sfw` 结尾的贴图资源；
  - 若同包存在同名无 `_sfw` 后缀的贴图，自动用后者图像数据替换 `_sfw` 贴图；
  - 自动生成标准 Mod 并安装；
  - 安装前自动备份原文件，可随时一键还原；
  - 未建立索引时也会自动扫描资源包。
- 按钮名称同步改为“一键创建去SFW贴图Mod”。

## 1.0.4

### 新增

- 一键创建去 SFW 模型 Mod（v1.0.5 起已改为只处理贴图，不再处理模型）：
  - 扫描所有以 `_sfw` 结尾的 3D 模型资源；
  - 若同包存在同名无 `_sfw` 后缀的模型，自动用后者数据替换 `_sfw` 对象；
  - 自动生成标准 Mod 并安装；
  - 安装前自动备份原文件，可随时一键还原；
  - 未建立索引时也会自动扫描资源包。

## 1.0.3

### 新增

- 支持扫描 TapTap 版《吉星派对》：
  - 识别 TapTap 客户端安装目录与多层版本目录；
  - 支持中文 exe：`吉星派对.exe`、`星引擎派对.exe`；
  - 自动定位 TapTap 版基础资源目录与 AppData 热更新缓存。
- 新增 GitHub Actions 自动构建发布：
  - 推送 `v*` 标签后自动在 Windows 上编译、打包并发布 Release。
- 界面提示与 README 更新为 Steam / TapTap 双平台说明。
- 增加旧 Mod 迁移：支持选择单个旧 `.bundle` / `__data` 或整个旧 Mod 文件夹
- 根据资源组、贴图名称和尺寸匹配当前版本资源，唯一匹配项可一键加入作品集
- 同名资源不再合并隐藏，列表会分别显示对应 bundle；有歧义的迁移项会安全跳过

### 修复

- 修复热更新缓存文件都被作品集记成 `__data`，导致第二次替换失败的问题
- 修复热更新资源加入作品集后无法显示游戏原图的问题
- 文件选择窗口始终置于应用前方
- 及时释放 UnityPy 资源句柄，迁移或预览后不再占用旧 bundle 和草稿文件

## 1.0.2

### 修复

- 合并 Steam 基础资源与 AppData 热更新缓存，不再因检测到缓存就忽略本地基础包
- 按最新 Addressables catalog 过滤过期资源，同名 bundle 优先使用 AppData 热更新版本
- 恢复未进入热更新缓存的手牌、技能卡等资源；当前版本可正确索引 71 张手牌
- 切换到空分类时清除上一次资源预览，避免右侧继续显示无关图片

## 1.0.1

### 修复

- 兼容新版 `%USERPROFILE%\AppData\LocalLow\feimo\AstralParty_CN\com.unity.addressables\AssetBundles` 热更新缓存
- 正确识别 Unity 缓存中的 `<缓存键>/<包哈希>/__data`，并映射回 Mod 使用的 `.bundle` 文件名
- 仅扫描最新 Addressables catalog 仍有效的缓存项，避免误用旧版本残留
- 安装、备份、还原、预览和作品集制作统一支持新版缓存布局
- 游戏资源版本变化后自动丢弃旧索引，避免显示已经失效的包

## 1.0.0

### 功能

- 本地安装、禁用、启用、卸载 Mod（文件夹、ZIP、RAR）
- 安装前自动备份，支持单 Mod 还原与一键全还原
- 浏览 Addressable 资源：贴图、文本、3D 模型、动画
- 贴图细分类与 PNG/JPG、文本、OBJ、动画数据导出
- 作品集支持多资源打包、裁剪换图、导出 ZIP 和直接安装
- HTML 前端由 C# WebView2 原生窗口承载，Python 仅提供本地接口

### 修复

- 作品集换图和裁剪换图后自动同步安装到游戏
- 从作品集移除项目时正确回退对应资源，不再残留旧 bundle
- 多个 Mod 修改同一 bundle 时，按最后启用顺序正确覆盖与恢复
- 禁止 WebView2 创建浏览器新窗口，并增加应用单实例保护
- ZIP/RAR 分析与安装复用临时目录，完成或退出后自动清理

### 安全说明

- 仅替换本地 bundle 文件，不注入进程、不读内存
- 默认自检只使用系统临时目录，不修改真实游戏与作品集
