# 吉星派对 Mod 助手 v1.2.0

这版加入文字编辑、动画替换和播放预览。在“动画”列表选中支持的角色动作后会直接播放，制作替换和作品集也能查看修改前后的效果。

## 新功能

- **文字替换**：编辑普通 TextAsset 和可识别的 FairyGUI 界面文字，支持查找、批量替换、恢复原文和作品集对照；保存时保留原编码和换行，并校验 JSON/XML 格式。
- **动画替换**：独立贴图序列可导入 GIF、APNG、动画 WebP 或 PNG 帧目录，按原帧数和尺寸适配；动画图片容器支持同格式替换。
- **动作预览**：支持单条 Sprite 引用轨的 AnimationClip，按真实关键帧顺序和时长播放。已验证 Cry、Hit（30 帧 / 1 秒）和 Walk-back（24 帧 / 0.8 秒）。
- **播放控制**：暂停、逐帧、进度、倍速、点画面放大，以及原版、候选替换和作品集前后对照。
- **同源动画数据**：支持 `.animbin` 导入校验及受支持片段的候选预览；图片替换作用于关联图集。

## 修复

- 修复“动画”页只显示静态图集、无法播放支持的角色动作的问题。
- 完善同包多项编辑、草稿移除和恢复，失败导入保留已有草稿。
- 连续编辑文字时保留原格式和编码，修复普通文字改成 JSON 后无法恢复及中文编码漂移；移除修改时精确恢复原始字节。
- 旧动画图集草稿无法播放时仍保留静态图集对照，并显示原因。
- 修复多文件 Mod 缓存不完整时仍显示启用成功的问题。
- 构建脚本保留旧输出，不再强制关闭运行中的助手；新旧版本可并排打开。

## 下载与使用

下载 **JiXingModHelper.zip**，完整解压到新目录，再双击 `JiXingModHelper.exe`。不要只复制 EXE，旁边的 `_internal` 目录也要保留。`SHA256SUMS.txt` 可用于校验下载文件。

首次打开先确认检测到游戏，再到“浏览资源”刷新索引。可以选择“动画”并搜索 `Cry` 测试播放；编辑文字可选择“文本”并搜索 `Common_fui`。保存到作品集只保存草稿，点击“安装到游戏”才会写入游戏资源。

旧版目录、备份和作品集请保留。若旧版助手已安装 Mod，先用旧版还原，再由新版安装，避免两份程序分别管理同一资源的备份。

## 支持范围与验证

已完成隔离自检、真实游戏资源副本的读写与恢复、动画时序验证，以及浏览器界面操作检查。尚未进入游戏进程验证修改后的最终显示。

本版不能完整还原 Unity 骨骼、多轨变换、场景依赖、粒子、Live2D、Spine 或复杂 FairyGUI 动效；无法解析的资源会显示具体原因。文字效果仍取决于游戏实际读取的资源与字体字形。程序不附带游戏资源、个人索引、作品集或备份。
